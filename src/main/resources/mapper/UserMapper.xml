<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aloha.seniorschedule.mapper.UserMapper">

    <select id="selectById" resultType="Users">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultType="Users">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <insert id="insert">
        INSERT INTO users (id, username, password, name, phone)
        VALUES (#{id}, #{username}, #{password}, #{name}, #{phone})
    </insert>

    <update id="update">
        UPDATE users 
        SET name = #{name}, phone = #{phone}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE users SET password = #{password}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>
